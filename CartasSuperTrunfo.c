#include <stdio.h>

// Desafio Super Trunfo - Países
// Tema 1 - Cadastro das Cartas
// Este código inicial serve como base para o desenvolvimento do sistema de cadastro de cartas de cidades.
// Siga os comentários para implementar cada parte do desafio.
//Teste larissa

int main() {
    printf("Desafio Super Trunfo!");
    
    // Sugestão: Defina variáveis separadas para cada atributo da cidade.
    // Exemplos de atributos: código da cidade, nome, população, área, PIB, número de pontos turísticos.
    
    // Cadastro das Cartas:
    // Sugestão: Utilize a função scanf para capturar as entradas do usuário para cada atributo.
    // Solicite ao usuário que insira as informações de cada cidade, como o código, nome, população, área, etc.
    
    // Exibição dos Dados das Cartas:
    // Sugestão: Utilize a função printf para exibir as informações das cartas cadastradas de forma clara e organizada.
    // Exiba os valores inseridos para cada atributo da cidade, um por linha.
    //Cadastramento das cartas
   char estado[6]; 'Parana';
   char codigo[5]; 'P01';
   char cidade[8]; 'Curitiba';
   int populacao = (1,773,718);
   float area = (434,892);
   float pib = (88,308,728,40); 
   int pontosturisticos = (23);


   char estado2[8];"Piaui";
   char codigo2[3];'P02';
   char cidade2[5];'teresina';
   int populacao2 = (102,649);
   float area2 = (1,391,981);
   float pib2 = (21,578,875,01);
   int pontosturisticos2 = (28);

   //Peço ao usuario para digitar os dados da primeira carta
   
   return 0;
}
